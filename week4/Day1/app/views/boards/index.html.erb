<h1>라밥 페이지에 오신걸 환영합니다!</h1>
<table class='table table-hover'>
    <thead>
        <tr>
            <th>글 번호</th>
            <th>제목</th>
            <th>보기</th>
            <th>수정</th>
            <th>삭제</th>
        </tr>
    </thead>
    <tbody>
        <% @boards.each do |board| %>
            <tr>
                <td><%=board.id%></td>
                <td><%=board.title %></td>
                <td><a href="/boards/<%=board.id%>">보기</a></td>
                <td><a href="/boards/<%=board.id%>/edit">수정</a></td>
                <td><a href="/boards/<%=board.id%>" data-method = "delete" data-confirm="삭제하시겠습니까?">삭제</a></td>
            </tr>
        <% end %>
    </tbody>
</table>
<a href="/boards/new" class="btn btn-danger">새글쓰기</a>